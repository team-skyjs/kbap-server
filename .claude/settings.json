{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Skill",
        "hooks": [
          {
            "type": "command",
            "command": "jq -c 'if (.tool_input.skill // \"\") == \"speckit-implement\" then {hookSpecificOutput: {hookEventName: \"PostToolUse\", additionalContext: \"speckit-implement 워크플로우를 실행 중입니다. 이 구현 작업을 모두 마친 뒤(모든 task 완료·빌드 그린) 마지막 단계로 ponytail-review 스킬(/ponytail-review)을 호출해 방금 변경한 코드의 과설계(불필요 추상화·speculative flexibility·재구현된 표준 라이브러리)를 점검하세요. 이번 세션에서 이미 ponytail-review 를 실행했다면 생략합니다.\"}} else empty end'",
            "statusMessage": "speckit-implement 완료 후 ponytail-review 안내"
          }
        ]
      }
    ]
  }
}
