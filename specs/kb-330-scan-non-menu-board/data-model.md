# Data Model: 스캔 2.0 — 메뉴판 아닌 사진의 빈 결과 처리

**변경 없음.** 엔티티·스키마·마이그레이션 무접촉. 빈 추출 결과는 기존 `ScanService` 동작대로 스캔 히스토리를 기록하지 않고(`recordHistory` 는 빈 추출 시 조기 반환), 스캔 횟수(`increaseScanCount`)는 기존과 동일하게 증가한다 — 이 부수 동작을 바꾸지 않는 것이 스펙 가정이다.
